# Dorvis Runtime Patch Manifest

This fork carries IdeaRoom-specific runtime patches on top of NousResearch
`hermes-agent` upstream. Keep this file current whenever a patch is added,
removed, rebased, or promoted.

Current upstream base for the carried branch: `fcbd1076a93841fa88855acce810e342a5b78101`
(official annotated release tag `v2026.8.19`, Hermes v0.20.5; previous base
`64702f8f91661149128ca1a721f7a0fd4c22113b`).

### 2026-08-24 rebase notes (64702f8f9 → fcbd1076a)

The upstream release is a large rollup, so every conflict was resolved at the
runtime contract rather than by choosing one side wholesale. Notable results:

- **Parity-first release with two reviewed improvements** — upstream's new
  native DOCX/XLSX/notebook reader is carried through the existing `read_file`
  surface with strict resource and archive/XML bounds, and the existing GPT /
  Codex execution-discipline prompt receives upstream's expanded read-back,
  count, and literal-preservation guidance. Newly shipped paid BFL/FLUX tools,
  hard loop caps, deterministic empty-response stopping, and the remaining
  optional tool/skill surfaces are explicitly disabled or left unselected.
  Their product adoption is evaluated separately under AE-226 after parity is
  proved.
- **Postgres session store, v22 → v26 re-audit** — upstream advances the state
  schema through v26, including prompt normalization, usage/display fields,
  hygiene state, and turn leases. The adapter now mirrors the exact v26 SQL
  surface (`cd2cb9ee351693e62e9dc8e425885a4a08148551d9577d506f4a11be4a715d5f`),
  ports the widened read/write/search/token-writer interface, and treats a
  configured Postgres DSN as mandatory. Ordinary boot observes an existing
  store without DDL and refuses v22, drift, or partial catalogs. Exactness is
  semantic: column types/nullability/defaults, complete constraint behavior,
  and index uniqueness/keys/order/opclasses/predicates/expressions are checked,
  with only the audited pg_trgm accelerator optional. The only accepted
  transition is the explicit, drained, transactional v22→v26 command; its dry
  run verifies the same exact predecessor contract without writes.
- **Postgres process ownership** — upstream's local-PID lock recovery cannot
  prove liveness across Fargate tasks. Postgres leases therefore expire by TTL
  only; a PID belonging to another task is never treated as local evidence.
- **SessionDB adapter shape** — `PgSessionDB(SessionDB)` remains the narrow
  compatibility adapter for this release. Upstream's patient write helper,
  read context, projected search, message-column cache, JSON reset markers,
  and token-writer drain semantics were carried into that seam instead of
  duplicating the full session-store module.
- **Agent lifecycle and observability** — Dorvis request provenance, complete
  usage accounting, terminal plugin metadata, memory/observer events, and
  background-review trace linking compose with upstream's new provider,
  reasoning, retry, verification, delegation, and stream-hook paths. The
  upstream control flow is retained; Dorvis hooks stay fail-open and additive.
- **Compression and context** — upstream native compaction, cache-boundary,
  protected-tail, and retry-state work is retained alongside Dorvis's stateful
  compression quality gate and boundary-continuity handoff. Neither side may
  reset the other's failure/anti-thrash state.
- **Gateway serving contract** — upstream's expanded API/gateway/profile
  lifecycle is retained while Dorvis keeps Postgres routing, exact storage
  attestation, readiness, drain mode, runtime footer/source identity, complete
  usage, and typed terminal failures. Default-home construction continues to
  use `open_session_db_for_home` so it cannot bypass DSN dispatch.
- **Container dependency closure** — the release's Node 26 runtime is retained.
  The Azure extra pairs `azure-identity==1.25.3` with `msal==1.37.0`, whose
  cryptography range is compatible with upstream's security-pinned
  `cryptography==50.0.0`; the image lock and `uv pip check` enforce the result.
- **Fork ref identity** — `dorvis/runtime` is the only automatic GHCR
  publisher. After that ref publishes the audited commit, fork `main` must be
  advanced by a raw fast-forward to the same SHA; a GitHub merge, squash, or
  rebase button would mint an unpublishable alias SHA and is not an accepted
  release path. Upstream-only mutating or high-cost default-branch automation
  (JS autofix, scheduled installer E2E, and inline evidence publication) is
  repository-gated so advancing the fork default branch cannot write or burn
  runners unexpectedly.

### 2026-07-21 rebase notes (a7f65e3bc → 64702f8f9)

Thirty-six live Dorvis commits were carried. The historical skill-review API
add/remove pair was not replayed because its net state is obsolete; runtime
skill writes remain ungated. Conflicted and notable resolutions:

- **Parallel tool execution** — upstream's concurrent dispatcher for safe
  built-in tools, model guidance, and Codex `parallel_tool_calls` transport
  flag were retained intact. MCP tools remain sequential by default and only
  run concurrently when their server explicitly declares
  `supports_parallel_tool_calls: true`; Dorvis task/session metadata keying is
  preserved on the same call path.
- **Request metadata and lifecycle hooks** — Dorvis request metadata was
  grafted through upstream's profile-scoped API-server runtime and delegate
  optional-argument forwarding. The lifecycle-hook signature audit found no
  removed Dorvis hook seam in this upstream range.
- **Stateful compression quality gate** — the Dorvis quality gate and
  completed/aborted observations now compose with upstream's `memory_context`
  argument, API-content sidecar, compression lock, and anti-thrash behavior.
- **Large output handoff** — complete sanitized output handoff is retained
  alongside upstream's explicit stdout truncation metadata for both local and
  remote code execution.
- **API-server lifecycle** — memory-provider cleanup, usage snapshots, and
  drain-cap agent tracking now run inside upstream's per-profile scope. The
  multiplexed route table and non-destructive run-stream transport TTL were
  retained; transport expiry never cancels a live executor-backed run.
- **Responses streaming failures** — an agent exception now retains the
  original structured `response.failed` envelope instead of referencing an
  unassigned result while classifying the error. A public Responses SSE
  regression test covers the exception path.
- **Complete run token attribution** — the run aggregate composes with
  upstream auxiliary session accounting, provider/cache-token normalization,
  Codex compaction bookkeeping, MoA context propagation, and the gateway's
  profile-scoped cleanup paths.
- Fork `main` was advanced by merging the audited upstream commit instead of
  fast-forwarding because fork-owned GHCR workflow commits make the two `main`
  histories intentionally divergent.

### 2026-07-09 rebase notes (2ebf9a90 → a7f65e3bc)

All carried patches survived; none were absorbed or dropped. Conflicted and
notable resolutions:

- **GPT-5.6 model family** — upstream's Sol/Terra/Luna registration, Codex
  OAuth discovery, native picker ordering, usage pricing, 272K Codex-route
  context metadata, and compaction-threshold coverage were accepted intact.
- **Request metadata and hook directives** — upstream generalized
  `pre_tool_call` from block-only results to block-or-approval directives.
  Dorvis `request_metadata` propagation now flows through that generalized
  helper and every backward-compatible wrapper, preserving upstream's
  fail-closed approval gate and the observability contract.
- **Stateful compression quality gate** — upstream added Codex app-server
  compaction modes and generalized the 85% Codex compaction autoraise through
  GPT-5.6. Both remain active alongside the Dorvis quality gate and its cache-
  busting config keys. Upstream's in-place hygiene-compaction fixes were kept.
- **MCP Hermes context keying** — upstream added per-server call lifecycle
  tracking plus stdio recycle/watchdog hardening. `_mark_server_call_started`
  and the opt-in `_meta.hermes.task_id` construction now both execute on the
  same call path.
- **Execute-code request context** — the carried per-turn budget now forwards
  upstream's `session_id` contract alongside `turn_id`, while omitting absent
  optional context so legacy remote-dispatch shims remain compatible.
- **Postgres session store** — upstream added the
  `idx_messages_active_null` legacy-SQLite repair index without bumping schema
  v19. Postgres already enforces `messages.active NOT NULL DEFAULT 1`; the
  adapter mirrors the partial index and accepts both audited surfaces after
  additive DDL lands under the bootstrap advisory lock. It continues to reject
  every unknown marker.
- **Postgres session store, v19 → v22 re-audit (AE-182)** — the GPT-5.6 rebase
  carried `hermes_state.py` to schema v22 without re-auditing the adapter, so
  the drift guard rejected `PgSessionDB` at every boot and the gateway silently
  fell back to the local SQLite/JSONL store. The v19 → v22 delta is additive:
  `sessions.compression_fallback_streak`, `sessions.profile_name`,
  `messages.effect_disposition`, `messages.api_content`, plus the
  `session_model_usage` (v20/v22) and `async_delegations` (v21) tables and
  their indexes. `PG_SCHEMA_SQL` mirrors them for fresh databases,
  `PG_EXPAND_SQL` widens existing ones with `ADD COLUMN IF NOT EXISTS`, and the
  persisted markers advance after the audited v19 predecessor is recognized.
  Upstream's SQLite-idiomatic `DO UPDATE SET col = col + excluded.col` upsert
  is ambiguous on Postgres and is replaced wholesale through
  `STATEMENT_OVERRIDES`. Separately, upstream `7aa21e336` made the API server
  open `SessionDB(db_path=home / "state.db")`, which bypasses the
  `HERMES_STATE_STORE_DSN` dispatch entirely; it now routes through
  `hermes_state.open_session_db_for_home`, which preserves the per-profile
  routing while letting the default home resolve through the dispatch.
- Hook-surface audit: the only matching upstream signature change was the
  `pre_tool_call` approval generalization above; no Dorvis lifecycle-hook call
  sites were removed.

### 2026-07-06 rebase notes (388268ec → 2ebf9a90)

All carried patches survived; none were absorbed or dropped. Conflicted and
notable resolutions:

- **Stateful compression quality gate** — conflicted in
  `agent/context_compressor.py` and `agent/conversation_compression.py`.
  Upstream stopped eagerly resetting `_last_summary_auth_failure` /
  `_last_summary_network_failure` at the top of `compress()` (data-loss fix
  #29559); the patch now follows that semantics and only resets its own
  quality-gate/diagnostic fields. The `context_compression_aborted` /
  `context_compression_completed` hook emissions were re-grafted into
  upstream's new try/finally compression-lock structure (lock lease refresher,
  in-place compaction #38763); the completed hook now also carries `in_place`.
  `quality_gate_enabled` is read via `getattr(..., False)` because upstream
  tests construct `ContextCompressor` via `__new__`, bypassing `__init__`.
- **MCP Hermes context keying** — additive collisions with upstream's
  `skip_preflight` doc row and new `TestFilterMCPChildren` tests; both sides
  kept.
- **Large output handoff** — trivial import-block collision in
  `tests/tools/test_code_execution.py`; both sides kept.
- Hook-surface audit: upstream added a `pre_verify` hook
  (`hermes_cli/plugins.py`) and now fires `on_session_end` memory extraction
  before soft-evicting finalizable sessions under LRU cache pressure
  (`gateway/run.py`). Both are additive; no fork hook call sites changed.
- Upstream consolidated gateway session metadata and the routing index into
  `state.db` (schema v19, `gateway_routing` table, `AsyncSessionDB` facade);
  `sessions.json` is now an optional legacy mirror. No carried patch touches
  that surface, but D6b (Postgres session store) builds on it.

## Patch Inventory

| Patch | Status | Purpose | Main files | Conflict surface | Owner |
|---|---|---|---|---|---|
| Request metadata and tool-dispatch observability | Promoted in fork history | Preserve request/session/turn metadata through hooks and tool execution so gateway, traces, and downstream plugins can correlate turns reliably. | `run_agent.py`, `model_tools.py`, `hermes_cli/hooks.py`, `hermes_cli/plugins.py`, `tools/delegate_tool.py` | Medium: agent loop and hook dispatch | IdeaRoom Agentic Systems |
| Native and AnyDoc document-reading hardening | Pending promotion | Expose upstream's native DOCX, XLSX, and IPYNB-to-text extraction through the existing bounded `read_file` contract while keeping blocked-path checks ahead of byte transport. Enforce fail-fast process concurrency, per-format byte and output budgets, bounded base64 transport that rejects a zero-byte result after a nonempty stat, strict OOXML ZIP/XML/relationship/cell limits, and safe notebook rendering that omits active HTML/widgets and sanitizes retained traceback text. Notebook JSON syntax/decode failures and parsed notebooks with no renderable cell structure fall back to the same already-bounded raw snapshot after their parser traceback graph is detached; oversize/I/O failures, malformed binary documents, and a future Python runtime without the private ZIP end-record helper fail cleanly. Valid dense OOXML content returns an explicitly truncated safe prefix when only a content/parser-view budget is exhausted, while ZIP, relationship, metadata, shared-string, and malformed-XML contracts remain fail closed. Preserve upstream's optional AnyDoc path for generic deployments, add a final 2,000,000-character conversion cap before `read_file` pagination/serialization, and retain `HERMES_DISABLE_ANYDOC` as a pre-import/pre-install managed-runtime kill switch whose truthy operator seal cannot be weakened by config. Managed overlays can bake and enable the exact converter version without relying on runtime downloads, while config-read failures still fail closed for only that converter. | `tools/read_extract.py`, `tools/file_operations.py`, `tools/file_tools.py`, `tests/tools/test_read_extract.py`, `tests/tools/test_file_operations.py`, `tests/tools/test_structured_read_file.py` | Medium: file-read dispatch, untrusted archive/XML parsing, memory and output bounds | IdeaRoom Agentic Systems |
| Hermetic Playwright container dependency | Pending promotion | Pin the root Playwright package and invoke its installed binary directly so the Chromium layer used by the runtime image cannot drift through `npx` package resolution between the tested and published build. | `package.json`, `package-lock.json`, `Dockerfile`, `tests-js/docker-playwright-pin.test.ts` | Low: container-only browser dependency installation | IdeaRoom Agentic Systems |
| Immutable GHCR publisher and base roots | Pending promotion | Build and label the arm64 runtime image exactly once, smoke that local image, fail closed on conflicting or unverifiable `sha-<commit>` tags, and export the read-back registry manifest digest that the parent stack pins. Automatic and manual publication have one canonical source (`dorvis/runtime`) so `main` cannot legitimize an accidentally rewritten alias SHA; pull requests to either integration branch run the same arm64 build/smoke path without package-write permission. Use BuildKit's config-based image ID instead of Docker's image-store-dependent inspect ID, and allow a bounded 45-second post-push read-back window without weakening exact config-digest equality. Pin both Debian build/runtime roots to one official immutable index digest, preserve idempotent reruns only when the remote config digest matches, and publish no mutable convenience alias. | `.github/workflows/idearoom-ghcr-publish.yml`, `scripts/ci/publish_immutable_image.py`, `tests/ci/test_ghcr_image_publish.py`, `Dockerfile` | Medium: container supply chain, GHCR publication, parent-image identity handoff | IdeaRoom Agentic Systems |
| Delegated code-execution boundary | Pending promotion | Preserve Dorvis's prior child-agent resource and side-effect boundary after upstream generalized delegated tool inheritance: leaf and orchestrator children cannot receive either `execute_code` or its composite `code_execution` toolset. | `tools/delegate_tool.py`, `tests/tools/test_delegate.py`, `website/docs/guides/delegation-patterns.md`, `website/docs/user-guide/features/delegation.md` | Low: delegated child tool filtering and model-facing delegation description | IdeaRoom Agentic Systems |
| Runtime footer preservation | Promoted in fork history | Preserve runtime footer usage fields across gateway streaming and response handling. | `gateway/runtime_footer.py`, `gateway/run.py`, `gateway/platforms/api_server.py` | Medium: gateway stream/result formatting | IdeaRoom Agentic Systems |
| Hindsight retained-chat tagging and document metadata | Promoted in fork history | Tag retained web chats with useful provenance and preserve document-level Hindsight metadata for recall/auditability, including chat/session/user/environment/profile fields when traffic comes through Hermes Web. | `agent/agent_init.py`, `agent/turn_context.py`, `plugins/memory/hindsight/__init__.py` | Low: Hindsight plugin provider surface and request metadata handoff | IdeaRoom Agentic Systems |
| Stateful compression quality gate | Promoted in fork history | Improve context-compression safety by preserving ledger/state details and validating compression quality. | `agent/context_compressor.py`, `agent/conversation_compression.py`, `agent/conversation_loop.py`, `agent/turn_context.py`, `agent/turn_finalizer.py` | High: agent context and compression flow | IdeaRoom Agentic Systems |
| Compression boundary handoff continuity | Pending promotion | Include the most recent protected assistant handoff as reconciliation-only context for the auxiliary summarizer, preventing completed work from surviving in the checkpoint ledger as pending while retaining the handoff verbatim in the primary-model tail. | `agent/context_compressor.py`, `tests/agent/test_context_compressor_boundary_continuity.py`, `tests/agent/test_context_compressor_summary_continuity.py`, `website/docs/developer-guide/context-compression-and-caching.md` | Medium: auxiliary summarizer prompt at the protected-tail boundary | IdeaRoom Agentic Systems |
| Concurrent-compaction convergence (AE-204) | Pending promotion | Make the losing side of a compression-lock race converge on the winner's result instead of returning the uncompacted transcript: bounded poll of the durable holder row, then adoption of the session's compacted live transcript re-based with this path's in-flight turn. Adoption requires positive proof that the holder compacted (a compaction summary in the stored transcript, per the compressor's own prefix predicate) so a holder that aborted having written nothing can never be laundered into a fake compaction. In-place winners refresh the current session; rotating winners rebind the losing live agent, memory manager, context engine, and flush cursor to the canonical child before its live tail continues. The carried internal `HERMES_COMPRESSION_LOCK_WAIT_SECONDS` compatibility override defaults to 60 seconds, treats invalid values as the default and negative values as zero, and is capped by the lock TTL; it remains an internal bridge rather than a user-facing configuration surface. Removes the divergent uncompacted copy that downstream transcript merges concatenated into a doubled context; a crashed or hung holder still falls back to the historical unchanged-messages behaviour under a distinct `compression skipped: holder timeout` tag. | `agent/conversation_compression.py`, `tests/agent/test_compression_concurrent_fork.py` | Medium: compression lock contention path in `compress_context` | IdeaRoom Agentic Systems |
| Responses store history doubling (staging 2026-08-11) | Pending promotion | Stop the /v1/responses stored conversation history from doubling every chained turn (h(n+1)=2h(n)+2; 2.2M-token requests by turn 6 on staging). Root cause: `_response_prefix_projection` stripped only underscore-prefixed bookkeeping, so the non-underscore `api_content` persistence sidecar that memory prefetch stamps on the current turn's user row (live Hindsight recall = every staging turn) defeated the expected-prefix match; the first turn then stored its user row twice, alternation repair merged those adjacent rows on the next turn so BOTH prefix checks failed, and the fallthrough re-embedded the entire prior history every turn. Fix: the projection now also ignores `api_content`; the turn-start detector gains a last-user-row anchor for repair-perturbed transcripts; and `_build_response_conversation_history`'s fallthrough can no longer re-embed prior history — user-row-bearing results are adopted as (possibly perturbed) authoritative transcripts, a suffix beginning at this turn's user row gets prior prepended once, and only user-row-free assistant/tool suffixes keep the concatenation shape. Compaction-summary adoption (AE-204), incomplete/failed snapshot shapes, and tool-only suffix semantics are unchanged. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Low: three static helpers on the Responses persistence path; a rebase touching `_response_prefix_projection`, `_response_messages_turn_start_index`, or the history builder should re-run the doubling regression tests (also resolves the risk note carried on the AE-196 row) | IdeaRoom Agentic Systems |
| Responses API Postgres store (D6a) | Promoted in fork history | Move the gateway Responses API store off EFS SQLite and into Postgres for concurrent web traffic; Postgres is durable state, does not use the old SQLite LRU cap, and can recover the required schema on fresh databases. | `gateway/platforms/api_server.py`, `gateway/platforms/response_store_pg.py` | Medium: gateway response-store boundary | IdeaRoom Agentic Systems |
| Responses schema and owner-lease hardening | Pending promotion | Make ordinary Postgres response-store boots read-only after an exact, versioned schema contract is installed, avoiding repeated DDL, migration locks, and backfills while still failing closed on altered trigger/FK semantics or missing core objects and lifecycle columns. Read the contract version independently before probing version-specific objects so an older runtime cannot mutate a future catalog that renamed a relation. Give every active response a renewable owner heartbeat; tolerate transient renewal errors only within the stale window, and atomically terminalize expired owned or post-migration legacy ownerless rows as `incomplete` / `owner_lost`. The same lease semantics apply to the SQLite compatibility store, and the live PostgreSQL Responses suite runs in the dedicated CI lane. | `gateway/platforms/api_server.py`, `gateway/platforms/response_store_pg.py`, `.github/workflows/idearoom-pg-state-ci.yml`, `tests/gateway/test_api_server.py`, `tests/gateway/test_response_store_pg.py`, `tests/gateway/test_response_store_pg_retention_unit.py` | Medium: response-store boot, schema contract, streaming-owner lifecycle, and CI coverage | IdeaRoom Agentic Systems |
| Cron fire-claim refusal recovery | Pending promotion | When ECS task-protection admission refuses a manually fired cron job, release only that invocation's owner-fenced fire claim and leave its prior status and repeat accounting untouched. A later scheduler may retry it; the refusal is no longer misreported as a completed failed run. | `cron/jobs.py`, `tools/cronjob_tools.py`, `tests/cron/test_claim_job_for_fire.py`, `tests/tools/test_cronjob_run_background.py` | Low: manual cron admission bookkeeping | IdeaRoom Agentic Systems |
| Browser raw-CDP supervisor disable | Promoted previously; carried in fork history | Let AWS Browserless raw-CDP stages disable the second persistent CDP supervisor client, avoiding an idle extra Chrome per browser-using chat. | `tools/browser_tool.py`, `tests/tools/test_browser_cdp_override.py` | Low: browser CDP setup path | IdeaRoom Agentic Systems |
| Browser CLI tenant isolation and lifecycle | Pending promotion | Adopt the upstream Browser CLI surface while retaining Browserless as Dorvis's multi-tenant CDP host. Bake the reviewed `browser-harness` 0.1.9 floor within a bounded pre-1.0 range into the immutable image under the expected `browser-use` command; derive opaque process/task-scoped daemon names so model-supplied friendly names cannot cross chat boundaries; cap one task at four daemons; give shared CDP calls their own tab; bound stdout memory and disk with workspace handoff; and connect task, cancellation, process-exit, and crash-recovery cleanup to every daemon with durable owner markers and a post-exec race sweep for late startup. | `Dockerfile`, `tools/browser_use_cli.py`, `tools/browser_tool.py`, `tests/tools/test_browser_use_cli.py`, `tests/tools/test_dockerfile_browser_cli.py` | Medium: replaces the model-facing browser tool surface and adds a process-global CLI daemon lifecycle, while preserving the existing Browserless tenancy boundary | IdeaRoom Agentic Systems |
| MCP Hermes context keying | Promoted previously; carried in fork history | Let trusted MCP servers opt in to `_meta.hermes.task_id` on `tools/call` so sandbox orchestrators can key one container per chat without trusting model arguments. | `tools/mcp_tool.py`, `tests/tools/test_mcp_tool.py`, `website/docs/reference/mcp-config-reference.md`, `website/docs/user-guide/features/mcp.md` | Low: opt-in MCP call metadata | IdeaRoom Agentic Systems |
| execute_code per-turn budget | Promoted previously; carried in fork history | Cap aggregate `execute_code` wall-clock time per assistant turn and pass `turn_id` into the tool handler, reducing runaway multi-call CPU exposure without adding a concurrency cap. | `model_tools.py`, `tools/code_execution_tool.py`, `tests/tools/test_code_execution.py`, `website/docs/user-guide/features/code-execution.md` | Medium: tool dispatch and execution timeout path | IdeaRoom Agentic Systems |
| Large output handoff | Carried in fork history | Replace oversized terminal and `execute_code` stdout truncation with sanitized file handoff references so JSON/CSV and other parseable output is not corrupted. | `tools/large_output_handoff.py`, `tools/code_execution_tool.py`, `tests/tools/test_large_output_handoff.py`, `tests/tools/test_code_execution.py`, `tests/tools/test_terminal_output_transform_hook.py` | Medium: tool output serialization and redaction | IdeaRoom Agentic Systems |
| Runtime source revision health field | Promoted previously; carried in fork history | Expose the loaded Hermes source commit through gateway health/status so promotion audits can compare running process code to the tested fork commit. | `gateway/status.py`, `gateway/platforms/api_server.py`, `tests/gateway/test_status.py`, `tests/gateway/test_api_server.py` | Low: health/status payloads | IdeaRoom Agentic Systems |
| Gateway readiness endpoint (AE-77) | Promoted in fork history | Expose a deploy/readiness check that reports API server state, runtime gateway state, active agents, runtime PID, and gateway owner for local, staging, production, and PR-environment validation. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Low: additive health/readiness endpoint | IdeaRoom Agentic Systems |
| Responses API usage transparency (AE-24) | Promoted in fork history | Preserve context-window usage, compaction count, cost estimate, and structured failure type in `/v1/responses` terminal envelopes for Hermes Web observability. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Low: additive Responses API envelope fields | IdeaRoom Agentic Systems |
| Responses compaction lifecycle SSE events | Pending promotion | Stream `response.context_compression.{started,completed,aborted,adopted}` on the `/v1/responses` SSE stream so Hermes Web can show a live "Compacting context" indicator during the 1–2 minute compaction stall and a durable "Context compacted" marker afterward. The compression wrapper invokes a per-run `agent._compression_event_callback` (attribute set by the API server's streaming branch, isolated from the observability plugin hook so neither failure path suppresses the other), and the SSE writer whitelists the payload to phase plus non-sensitive counters — free-text hook fields such as `abort_reason` (raw provider error text) and `model`/`focus_topic` never reach the client stream. Additive event types: spec-conformant Responses clients that switch on known event names skip them unchanged. | `agent/conversation_compression.py`, `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py`, `tests/agent/test_compression_event_callback.py` | Low: additive SSE event types on the Responses streaming path | IdeaRoom Agentic Systems |
| Responses usage cache split and honest context reporting | Pending promotion | Publish the prompt-cache split on the `/v1/responses` usage envelope and stop fabricating a context reading. The envelope's aggregate `input_tokens` is prompt-inclusive, so it now carries additive `cache_read_tokens` / `cache_write_tokens` (parent-scope counters only — the auxiliary and delegated buckets do not track cache, so a consumer deriving fresh input overstates it slightly, in the conservative direction) letting Hermes Web headline fresh input + output instead of the alarming total-processed number. The context trio (`context_used`, `context_percent`, `context_max`) is now published only when the context engine has a real measured reading: the previous fallback substituted the run-cumulative `session_total_tokens` — tokens processed across the whole run, not a context size — which clamped to 100% and faked a "compaction imminent" warning. A `0` (no provider response yet) and the `-1` post-compaction sentinel are both treated as "not measured", and `context_max` is omitted with the rest so consumers render "not reported" rather than a misleading 0%. `compressions` reporting is unchanged and independent. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Low: additive Responses API envelope fields plus one omission branch in `_session_usage_snapshot`; extends the AE-24 surface | IdeaRoom Agentic Systems |
| Responses cancel route (AE-224) | Pending promotion | Give an abandoned streaming `/v1/responses` turn a way to be stopped without destroying the record it left behind. `POST /v1/responses/{id}/cancel` interrupts the agent's tool-calling loop and persists a terminal `cancelled` envelope that `GET /v1/responses/{id}` and `previous_response_id` chaining still read; `DELETE` was never a substitute because it removes exactly the evidence a recovering client came back for. Statuses are explicit: 200 + the cancelled response object, 409 for an already-terminal response (a repeated cancel included), 404 with an OpenAI error body for an unknown id, and retryable 503 when this gateway cannot route/acknowledge the interrupt or commit the state transition. Because the agent runs on an executor thread that no asyncio cancellation reaches, `request_hard_interrupt` is the operative path: it uses the hard-stop ABI so explicit cancellation still wins while ordinary interrupts are masked during compression, with a legacy fallback for older agent implementations. `_inflight_responses` publishes the streaming turn's agent ref and task for the route's lifetime, and `agent_ref` carries a pending-interrupt slot so a cancel arriving during agent construction is honored by the executor thread instead of dropped. Cancellation is owner-routed: each stored stream carries a durable gateway owner/epoch; a sibling or orphaned active row returns retryable 503 without mutation because that process cannot acknowledge an interrupt. The owner returns 200 only after interrupt acknowledgement and a terminal CAS, and monotonic terminal writes prevent late completion/failure from replacing `cancelled`. `DELETE` returns 409 while active; terminal deletion atomically removes the response and conversation mapping. The Postgres migration takes both response tables behind one write lock while it adds lifecycle columns/defaults, installs the old-writer classifier, backfills legacy rows, removes dangling mappings, and establishes the cascading FK; it validates the function, trigger, and FK semantics rather than accepting namesakes. Mixed-version database fences reject an old gateway's semantic upsert against an owned row (so its cancel path fails instead of returning false containment) and preserve an active owned row plus mapping from its DELETE path; owner-null legacy rows and accessed-at-only touches remain compatible, while current owner/epoch CAS writes receive transaction-local authorization that cannot leak through the connection pool. Non-streaming `/v1/responses` has nothing to cancel — it mints its id only after the run returns. Every SSE abandonment path hard-contains executor work before best-effort persistence: client disconnect, server cancellation, writer failure, failed `StreamResponse.prepare`, and a persistently unavailable store cannot strand a provider/tool thread behind a terminal or missing record. The handler also backstops failures that occur before the writer installs its lifecycle guard. A terminal envelope has to account for what the run spent: the writer's `usage` local is only filled in when the agent task returns, so `cancelled`, `incomplete`, and the writer-crash `response.failed` event read the live agent's committed counters through `agent_ref[0]` rather than publishing zeros for a run that really paid for completed turns. The provider request in flight at the interrupt returned no usage and cannot be reconstructed, so that reading is the last committed cumulative — an honest undercount, labelled `completeness: partial` with a `run_interrupted_before_completion` warning. An interrupt landing before anything was committed keeps the aggregate's own `unavailable` rather than a bare zero a consumer reads as a measured one, and an agent that finished before the writer picked up its result has its own complete snapshot published from the resolved task — `_run_agent` clears `agent_ref` on the executor thread before the future resolves, so that window has no live agent to read. With `store=false` there is no stored envelope to read terminality from, so the in-flight entry records it directly and an already-finished turn answers 409 instead of reporting containment of a run that was already over. | `gateway/platforms/api_server.py`, `gateway/platforms/response_store_pg.py`, `tests/gateway/test_api_server.py`, `tests/gateway/test_response_store_pg.py`, `tests/gateway/test_response_store_pg_retention_unit.py`, `website/docs/user-guide/features/api-server.md`, `website/docs/developer-guide/programmatic-integration.md` | Medium: the route now relies on additive response lifecycle columns, a locked blue/green compatibility migration with exact function/trigger/FK/fence validation, transaction-local write authorization, hard-stop lifecycle containment, and live cancellation state-machine paths; a rebase touching `_write_sse_responses`, response-store writes/deletes, or `_run_agent`'s `agent_ref` contract should re-run the API, fake-Pg, and live-Pg race suites | IdeaRoom Agentic Systems |
| Responses streaming failure envelope (AE-162) | Pending promotion | Preserve the original typed `response.failed` event when the streaming agent task raises before returning a result. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Low: Responses SSE terminal failure path | IdeaRoom Agentic Systems |
| Complete run token attribution (AE-140) | Pending promotion | Report parent, attributable auxiliary `call_llm`, and delegated child-session input/output usage as separate buckets plus a complete/partial/unavailable run aggregate. Distinct provider responses are identity-deduplicated, known invalid-response/retry spend is retained, dispatched attempts that end without terminal usage fail closed to partial, MoA advisor and internal chat/Codex stream retries carry their uncertainty forward, and nested delegation rolls up once per tree edge. Codex app-server usage treats cached-read and cache-write tokens as prompt subsets, matching provider semantics and preventing those buckets from being counted twice in prompt, total, and cost aggregates; this intentionally lowers previously inflated Codex usage totals without changing the measured prompt size used for compression. | `agent/runtime_usage.py`, `agent/auxiliary_client.py`, `agent/chat_completion_helpers.py`, `agent/codex_runtime.py`, `agent/conversation_compression.py`, `agent/conversation_loop.py`, `agent/moa_loop.py`, `agent/usage_pricing.py`, `agent/agent_init.py`, `run_agent.py`, `tools/delegate_tool.py`, `gateway/platforms/api_server.py`, `tests/agent/test_runtime_usage_attribution.py`, `tests/agent/test_auxiliary_client.py`, `tests/agent/test_codex_app_server_persist.py`, `tests/run_agent/test_codex_app_server_compaction.py`, `tests/run_agent/test_moa_loop_mode.py`, `tests/run_agent/test_run_agent.py`, `tests/run_agent/test_run_agent_codex_responses.py`, `tests/run_agent/test_streaming.py`, `tests/tools/test_delegate.py`, `tests/gateway/test_api_server.py` | Medium: primary/auxiliary dispatch and retry accounting, MoA and stream helpers, app-server compaction, delegation aggregation, Responses usage envelope | IdeaRoom Agentic Systems |
| Complete observer operation coverage (AE-168) | Pending promotion | Emit backend-neutral, fail-open lifecycle events for every central auxiliary provider attempt (including consumed streams and retries), normalized guardrail outcomes, committed built-in and accepted provider memory writes, while retaining explicit subagent lifecycle correlation for independently owned linked traces in the platform adapter. | `agent/runtime_usage.py`, `agent/auxiliary_client.py`, `agent/memory_manager.py`, `agent/background_review.py`, `tools/approval.py`, `tools/delegate_tool.py`, `hermes_cli/plugins.py`, `tests/agent/test_runtime_usage_attribution.py`, `tests/agent/test_memory_provider.py`, `tests/run_agent/test_moa_streaming.py`, `tests/tools/test_approval_plugin_hooks.py`, `tests/tools/test_delegate.py`, `docs/observability/README.md` | Medium: central auxiliary dispatch, approval return paths, background memory synchronization, delegation context propagation, and observer hook registry | IdeaRoom Agentic Systems |
| Observer usage, cost, and request provenance (AE-169) | Pending promotion | Send normalized token, cache, reasoning, cost, estimate-source, and estimate-status fields to observer hooks for every primary and attributable auxiliary provider dispatch. Preserve deterministic pre-sanitization request-field and parameter digests when bounded hook payloads truncate large prompts or tool schemas. Unknown pricing remains explicitly unknown instead of being coerced to zero, while known zero cost remains representable. | `run_agent.py`, `agent/runtime_usage.py`, `tests/agent/test_observer_usage_cost.py`, `tests/agent/test_runtime_usage_attribution.py`, `tests/run_agent/test_run_agent.py` | Low: additive observer-hook payload fields at the existing centralized usage seams | IdeaRoom Agentic Systems |
| Terminal plugin response metadata (AE-170) | Pending promotion | Return bounded, JSON-safe terminal metadata from trusted `on_session_end` plugins through both streaming and non-streaming Responses envelopes. Malformed, conflicting, and oversized metadata is ignored fail-open; first writer wins so the platform can durably reconcile one deterministic trace manifest without making observability part of execution correctness. | `agent/turn_finalizer.py`, `gateway/platforms/api_server.py`, `tests/agent/test_turn_finalizer_response_metadata.py`, `tests/gateway/test_api_server.py` | Low: additive terminal result and Responses metadata fields | IdeaRoom Agentic Systems |
| Memory recall traceability (AE-194) | Pending promotion | Name the individual memories a turn saw instead of only counting characters. The Hindsight provider keeps per-result structure (`id`, snippet `text`, `score`, `type`, `document_id`) beside the recall text it already injected, the memory manager drains it once per turn through the new optional `MemoryProvider.consume_prefetch_memories()` seam, and it reaches observers as an additive `memories` key on `memory_context_injected` plus a bounded `dorvis_memory_recall` entry in the terminal `response_metadata` (same channel as the trace manifest, omitted when nothing was injected). Also makes the Hindsight config env fallback and config parse failures loud at WARNING instead of silent (AE-182 posture). Injection text, scrubbing, and prompt bytes are unchanged. | `plugins/memory/hindsight/__init__.py`, `agent/memory_provider.py`, `agent/memory_manager.py`, `agent/turn_context.py`, `agent/conversation_loop.py`, `agent/turn_finalizer.py`, `hermes_cli/hooks.py`, `docs/observability/README.md`, `tests/plugins/memory/test_hindsight_provider.py`, `tests/plugins/test_hindsight_memory.py`, `tests/agent/test_memory_provider.py`, `tests/agent/test_turn_finalizer_response_metadata.py`, `tests/run_agent/test_run_agent.py` | Low: additive observer payload fields, one additive optional provider method, and an additive terminal metadata key; `_recall_context_text` became a wrapper over the tuple-returning `_recall_context`, so a rebase that touches the Hindsight prefetch path should re-check both | IdeaRoom Agentic Systems |
| Recall observation survives alternation repair (AE-196) | Pending promotion | Keep the per-turn memory-recall observation alive on platforms whose history needs role-alternation repair. `repair_message_sequence` compacts `messages` in place between the turn prologue and the `api_messages` build, so the loop's `current_turn_user_idx` went stale — on every api_server (web) follow-up turn it pointed past the new end, the current-turn branch never matched, and `memory_context_injected` never fired (no `memory.retrieve`/`memory.inject` observations, no `dorvis_memory_recall` response metadata, no web recall block) even though the model still received the memories through the replayed `api_content` sidecar. The loop now re-anchors that index by object identity (the property repair already guarantees for survivors, and which the flush-cursor recompute relies on), falling back to content matching when repair merged this turn's message into an earlier user row. Repair semantics and the flush cursor are untouched; the persist-override tracker follows only when identity survived. Also widens the traceability channel for the recalled-memory UI: the Hindsight structured snippet cap goes 280 -> 2000 chars with an additive `text_truncated` flag on records that were cut, and the bounded `dorvis_memory_recall` envelope goes 16 KiB -> 48 KiB with an additive `memories_truncated` flag when whole trailing records are shed (`count` still reports the runtime count). Injection bytes, scrubbing, and prompt bytes are unchanged. | `agent/conversation_loop.py`, `agent/turn_context.py`, `agent/turn_finalizer.py`, `plugins/memory/hindsight/__init__.py`, `docs/observability/README.md`, `tests/agent/test_api_content_sidecar.py`, `tests/agent/test_turn_finalizer_response_metadata.py`, `tests/plugins/memory/test_hindsight_provider.py`, `tests/run_agent/test_run_agent.py` | Low: one re-anchor of an existing loop-local index at the existing repair seam, plus additive observer/metadata fields and two raised bounds. The distinct `api_content`-driven Responses transcript duplication is resolved by the “Responses store history doubling” patch above; a rebase touching either the alternation-repair anchor or the Responses projection/history builder should re-run both regression families. | IdeaRoom Agentic Systems |
| Background-review invocation tracing (AE-172) | Pending promotion | Snapshot trusted first-party request metadata before the asynchronous post-session memory review starts, detach that review from the completed foreground trace, and carry a validated `caused_by` link into its own canonical invocation. Snapshot and copy failures degrade to unlinked legacy tracing without interrupting the review. Preserve provider-reported total-token disagreement as explicit auxiliary-usage reconciliation evidence instead of silently discarding it. | `agent/background_review.py`, `agent/runtime_usage.py`, `tests/run_agent/test_background_review.py`, `tests/agent/test_runtime_usage_attribution.py` | Low: additive metadata on the existing daemon review handoff and additive observer usage fields | IdeaRoom Agentic Systems |
| Skill review gate removal (AE-81) | Promoted in fork history | Keep AWS runtime-created skills in the current free-write posture by removing the pending skill review API surface and tests that would reintroduce the old approval queue. | `gateway/platforms/api_server.py`, `tests/gateway/test_api_server.py` | Medium: API surface and product workflow posture | IdeaRoom Agentic Systems |
| API server memory cleanup (AE-47) | Carried in fork history | Ensure API-server and runs flows clean up memory providers when runs end and skip next-turn memory prefetch when a client sends a follow-up before the previous turn finishes. | `gateway/platforms/api_server.py`, `run_agent.py`, `tests/gateway/test_api_server.py`, `tests/gateway/test_api_server_runs.py`, `tests/run_agent/test_memory_sync_interrupted.py` | Medium: gateway run lifecycle and memory provider cleanup | IdeaRoom Agentic Systems |
| Local Codex auth adoption | Carried in fork history | Recover local parity on fresh profile volumes by adopting a valid mounted Codex CLI auth snapshot only when `CODEX_HOME` is explicit and Hermes Codex auth state is absent. | `hermes_cli/auth.py`, `tests/hermes_cli/test_auth_codex_self_heal.py` | Low: Codex OAuth credential resolution | IdeaRoom Agentic Systems |
| Postgres session store (D6b, AE-115) | Carried in fork history | Move the shared session store (`state.db`) off EFS SQLite into a dedicated `hermes_state` Postgres schema so blue/green gateway deploys can run two tasks against shared state (ADR 0177 in idearoom-agents). `SessionDB.__new__` dispatches to `PgSessionDB` when `HERMES_STATE_STORE_DSN` is set and no explicit `db_path` is given; a configured DSN is mandatory and initialization failures stop boot rather than falling back. `PgSessionDB` inherits SessionDB method bodies over a SQLite→Postgres statement translator (tsvector/ILIKE replace FTS5/trigram search) and pins the upstream `SCHEMA_VERSION` + DDL surface hash. Ordinary boot creates only a truly absent fresh schema and never migrates an existing one. Includes explicit `state.db`→Postgres and drained v22→v26 migration entrypoints. | `hermes_state.py` (the `__new__` seam plus the `open_session_db_for_home` helper), `hermes_state_pg.py`, `gateway/platforms/api_server.py` (per-profile session-DB routing must not pass an explicit `db_path` for the default home), `scripts/migrate_state_to_postgres.py`, `scripts/migrate_pg_schema_v22_to_v26.py`, `tests/fixtures/hermes_state_pg_v22.sql`, `tests/gateway/test_session_store_pg.py`, `tests/gateway/test_session_store_pg_unit.py`, `tests/conftest.py` | High: any upstream change to `SCHEMA_SQL` / `DEFERRED_INDEX_SQL`, migration order, SessionDB SQL dialect, write patience, read context, token writer, or search projection requires re-auditing `hermes_state_pg.py` and updating its pinned `EXPECTED_SCHEMA_VERSION` / `EXPECTED_SCHEMA_SURFACE_SHA256`. The boot guard refuses every unknown or partial catalog. Any new `SessionDB(db_path=...)` call site on a gateway path silently defeats store dispatch and must be reviewed. | IdeaRoom Agentic Systems |
| Gateway drain mode (AE-117) | Carried in fork history | Enable drain-based blue/green deploys (parent-repo ADR 0177): a draining gateway refuses new run launches with `503 {"error": {"code": "gateway_draining"}}`, finishes in-flight runs and their established SSE streams, reports `{draining, active_runs}` on readiness, and self-exits at zero (hard cap `HERMES_DRAIN_CAP_SECONDS`, default 3600, with cooperative terminal fencing at the cap). A process-global ECS protection client accounts for the API adapter plus non-overlapping relay/startup-resume and cron sources during steady state and drain. Every API request is visible before its first await, every relay/resume turn before agent setup, and every built-in or externally fired cron job through completion; execution waits for an acknowledged protection SET and API failures return retryable `task_protection_unavailable`. Protection requests use a bounded short timeout and fail-closed retry backoff so repeated admissions cannot serialize behind long ECS-agent stalls. SET/renew/release transitions are serialized against the current aggregate work count, and every admission advances a lock-protected epoch so an event-loop-owned idle snapshot cannot release protection after newer work appears. Pending-to-running handoffs publish their destination count before releasing the reservation, active-work callback failures count conservatively busy, and the one-way drain latch closes every admission source before the coordinator's final zero release. Internal notifications arriving after the latch closes are silently dropped instead of bypassing drain or presenting a misleading user retry. Release wakes promptly, renewals are capped at half the ECS expiry, and any ambiguous mutation response invalidates the local protected belief so a later admission must re-protect. Outside ECS this remains a clean no-op. Triggers: `HERMES_DRAIN_ON_SIGTERM`-gated unplanned SIGTERM, or authenticated `POST /admin/drain`. Reuses upstream machinery: the drain-control marker (`gateway/drain_control.py`) quiesces relay admission, runner/adapter terminators hard-interrupt the work each source counted, and self-exit goes through `runner.stop()`'s graceful shutdown. | `gateway/drain_mode.py`, `gateway/run.py`, `gateway/platforms/api_server.py`, `cron/scheduler.py`, `tests/gateway/test_drain_mode.py`, `tests/gateway/test_api_server_active_work_drain.py`, `tests/gateway/test_cron_active_work_drain.py`, `tests/cron/test_parallel_pool.py`, `scripts/drain_mode_sim.py`, `website/docs/reference/environment-variables.md` | Medium: API/relay/cron admission bookkeeping, ECS task-protection state transitions, api_server readiness, and runner shutdown/drain hooks | IdeaRoom Agentic Systems |

## Maintenance Rules

- Prefer additive modules and narrow hook-point edits over broad upstream rewrites.
- Keep model-visible tool schemas stable unless the product behavior truly changes.
- Add or update focused tests before implementation changes.
- After rebasing, run the focused tests named by each affected patch plus the
  parent repo promotion gate before deploying the fork runtime.
- When a source-only patch is committed and promoted, update its status here and
  the parent repo's `packages/hermes-config/hermes-fork-state.json`.
